<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppMain" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowActionBar">false</item>
    </style>

    <style name="switchOptionsButtonReleased">
        <item name="android:starStyle">?attr/borderlessButtonStyle</item>
        <item name="android:background">@color/switch_released</item>
    </style>

    <style name="switchOptionsButtonPressed">
        <item name="android:buttonStyle">?attr/borderlessButtonStyle</item>
        <item name="android:background">@color/switch_pressed</item>
        <item name="android:height">0dp</item>
    </style>

</resources>
